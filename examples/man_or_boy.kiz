fn A(k, x1, x2, x3, x4, x5)
    fn B()
        k = k - 1
        return A(k, B, x1, x2, x3, x4)
    end
    if k <= 0
        return x4() + x5()
    else
        return B()
    end
end

print( A( 10, || 1, || -1, || -1, || 1, || 0 ) )